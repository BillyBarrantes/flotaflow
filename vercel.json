{
  "buildCommand": "cd console && pnpm run build",
  "outputDirectory": "console/dist",
  "installCommand": "corepack enable && cd console && pnpm install --no-frozen-lockfile",
  "cleanUrls": false,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
