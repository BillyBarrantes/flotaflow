{
  "buildCommand": "corepack pnpm --filter @flotaflow/console run build",
  "outputDirectory": "console/dist",
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.4 --activate && corepack pnpm install --no-frozen-lockfile",
  "cleanUrls": false,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
