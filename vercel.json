{
  "buildCommand": "pnpm --filter @flotaflow/console run build",
  "outputDirectory": "console/dist",
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.9 --activate && pnpm install --no-frozen-lockfile",
  "cleanUrls": false,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
