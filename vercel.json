{
  "buildCommand": "cd console && pnpm run build",
  "outputDirectory": "console/dist",
  "installCommand": "cd console && pnpm install --no-frozen-lockfile",
  "cleanUrls": false,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
